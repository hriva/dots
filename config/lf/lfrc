# Options
# set previewer ~/.config/lf/preview-sandbox.sh
set preview true
set sixel true
set cursorpreviewfmt ""
set info size
set period 1  # Polling for updates
set dircounts
set drawbox true
set icons true

# Mappings
map i $LESSOPEN='| ~/.config/lf/scope-lf-wrapper.sh %s' less -R $f
map p : paste; clear

# Trashcli
cmd trash %trash-put $fx
map D trash
